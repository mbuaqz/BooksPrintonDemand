// Point this at your deployed M-Pesa backend (see /backend in this repo).
// Leave it blank ("") to disable live M-Pesa push and fall back to
// manual Till payment + confirmation code entry only.
window.APP_CONFIG = {
  API_BASE_URL: "" // e.g. "https://books-pod-ke-api.onrender.com"
};
